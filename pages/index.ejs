<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
    />
    <title><%= title %></title>
  </head>
  <body>
    <div class="container">
      <% if(created) { %>
      <div class="alert alert-primary">Your note was added!</div>
      <% } %>
      <h1>Main Page: <%= title %></h1>
      <form action="/" method="post" class="mb-3 form-control">
        <div class="form-group mb-3">
          <input type="input" class="form-control mb-3" name="title" required />
          <button type="submit" class="btn btn-primary">Create request!</button>
        </div>
        <div class="card">
          <% if(notes.length){ %>
          <ul class="form-group w-100">
            <% for(let i=0; i < notes.length; i++){ %>
            <li
              class="form-group-item d-flex justify-content-between align-items-center p-3 mb-2 border-bottom"
            >
              <span> <%= notes[i].title %> </span>

              <div>
                <button
                  class="btn btn-danger"
                  data-edit="edit"
                  data-id="<%= notes[i].id%>"
                  data-title="<%= notes[i].title %>"
                >
                  Edit
                </button>
                <button
                  class="btn btn-warning"
                  data-type="remove"
                  data-id="<%= notes[i].id%>"
                >
                  &times;
                </button>
              </div>
            </li>

            <% } %>
          </ul>
          <%} else { %>
          <p>No notes! You can add yours!</p>
          <%}%>
        </div>
      </form>
    </div>

    <script src="/app.js"></script>
  </body>
</html>
